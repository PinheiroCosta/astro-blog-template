services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "4321:4321"

    volumes:
      - .:/app
      - node_modules:/app/node_modules

    stdin_open: true
    tty: true

volumes:
  node_modules:
